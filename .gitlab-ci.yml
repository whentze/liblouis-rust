stages:
  - build

before_script:
  - apt-get update -y
  - apt-get -y install liblouis-dev

rust-stable:
  stage: build
  image: rust:stretch
  script:
    - cargo build
    - cargo test

variables:
  RUST_BACKTRACE: "1"
  RUST_LOG: "info"